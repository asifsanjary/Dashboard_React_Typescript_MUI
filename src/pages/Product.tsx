import { Stack } from '@mui/material';
import React from 'react';

const Product = () => {
    return (
        <Stack
            direction="column"
            spacing={4}
        >
        </Stack>
    );
};

export default Product;